//  WAP to perform the addition of two integer and display their result
#include <stdio.h>
#include <conio.h>

int main()
{
    // Step 1: Declarer the variable
    int a = 20, b = 30;

    // step 2: build the logic
    int result = a + b;

    // step 3: Display the Result
    printf("the sum of two number is =%d", result);

    return 0;
}
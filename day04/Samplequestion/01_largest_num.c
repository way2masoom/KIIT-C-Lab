// WAP to find the largest between two number
#include <stdio.h>
int main()
{
    int num1, num2;
    printf("Enter the First Number:");
    scanf("%d", &num1);

    printf("Enter the First Number:");
    scanf("%d", &num1);

    if (num1 > num2)

        printf("Num1 is greater");

    else
        printf("Num2 is greater");

    return 0;
}
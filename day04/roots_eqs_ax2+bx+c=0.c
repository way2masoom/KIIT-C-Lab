// WAP to find the roots if a quadratic equation ax2+bx+c=0 using if else statment
#include <stdio.h>
int main()
{
    float a, b, c;
    printf("\n Enter the Value of A,B and C:");
    scanf("%d%d%d", &a, &b, &c);

    








    return 0;
}
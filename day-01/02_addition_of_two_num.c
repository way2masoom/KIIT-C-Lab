// 	Addition of two number

/*
Algorithm
1. Store two number in separate variable, a=20 and b=30;
2. crate a new variable result and store the sum of a and b: result=a+b;
3. Display the value of the result

*/

#include <stdio.h>
#include <conio.h>

int main()
{
    // Step 1: Declarer the variable
    int a = 20, b = 30;

    // step 2: build the logic
    int result = a + b;

    // step 3: Display the Result
    printf("the sum of two number is =%d", result);

    return 0;
}